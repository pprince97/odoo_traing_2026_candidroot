<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="stage_list_view" model="ir.ui.view">
        <field name="name">project.stage.list</field>
        <field name="model">project.stage</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="project_ids" widget="many2many_tags"/>
                <field name="fold_state"/>
            </list>
        </field>
    </record>

    <record id="stage_form_view" model="ir.ui.view">
        <field name="name">project.stage.form</field>
        <field name="model">project.stage</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="project_ids" widget="many2many_tags"/>
                        <field name="fold_state"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="project_stage_action" model="ir.actions.act_window">
        <field name="name">Stage</field>
        <field name="res_model">project.stage</field>
        <field name="view_mode">list,form</field>
    </record>
    <menuitem name="Project Management" id="project_root"/>
    <menuitem name="Stage" id="project_stage" parent="project_root" action="project_stage_action"/>
</odoo>