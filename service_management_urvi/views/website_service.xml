<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="addons_in_account_service" inherit_id="portal.portal_my_home">
        <xpath expr="//div[hasclass('o_portal_doc_spinner')]" position="before">
            <div class="o_portal_category row g-2 mt-4">
                <t t-call="portal.portal_docs_entry">
                    <t t-set="icon" t-value="'/portal/static/src/img/portal-connection.svg'"/>
                    <t t-set="title">My Service Requests</t>
                    <t t-set="text">Your Service Requests</t>
                    <t t-set="url" t-value="'/my/request'"/>
                    <t t-set="config_card" t-value="True"/>
                </t>
            </div>
<!--                <div class="o_portal_category row g-2 mt-4">-->
<!--                    <a href="/create/request" class="btn btn-primary p-0 mt-3"><i-->
<!--                            class="fa fa-fw fa-info-circle"/>Book Request Service-->
<!--                    </a>-->
<!--                </div>-->
        </xpath>
    </template>

    <template id="create_request_record" name="Contact Details">
        <t t-call="website.layout">
            <div id="wrap" class="container mt-4">
                <div class="row">
                    <div class="col-lg-6 offset-lg-3" id="profile_info">
                        <h2 class="mb-4">Create Service Request</h2>
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                        <!-- Name -->
                        <div class="mb-3">
                            <label for="name" class="form-label">Full Name</label>
                            <input type="text" name="name" class="form-control" id="name"
                                   required="required"/>
                        </div>

                        <!-- Email -->
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" name="email" class="form-control" id="email"
                                   required="required"/>
                        </div>

                        <!-- Phone -->
                        <div class="mb-3">
                            <label for="phone" class="form-label">Phone</label>
                            <input type="tel" name="phone" class="form-control" id="phone"
                            />
                        </div>

                        <!-- Gender (Assuming a selection field) -->
                        <div class="mb-3">
                            <label for="gender" class="form-label">Gender</label>
                            <select name="gender" class="form-select" id="gender">
                                <option value="">Select...</option>
                                <option value="male">Male</option>
                                <option value="female">Female</option>
                                <!--                                    <option value="other" t-att-selected="partner.gender == 'other'">Other</option>-->
                            </select>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="country_id" class="form-label">Country</label>
                                <select name="country_id" class="form-select form-select-sm" id="country_id">
                                    <option value="" hidden="1">Select Country...</option>
                                    <t t-foreach="request.env['res.country'].search([])" t-as="c">
                                        <option t-att-value="c.id">
                                            <t t-out="c.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label for="state_id" class="form-label">State</label>
                                <select name="state_id" class="form-select form-select-sm" id="state_id">
                                </select>
                            </div>

                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3" id="city_id">
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="zip" class="form-label">ZIP</label>
                                <input type="text" name="zip" class="form-control" id="zip"/>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="street" class="form-label">Address</label>
                            <input type="text" name="street" class="form-control" id="street"/>
                        </div>

                        <div class="col-12">
                            <label class="form-label">Upload Document (PDF)</label>
                            <div class="input-group">
                                <span class="input-group-text">
                                    <i class="fa fa-file-pdf-o"/>
                                </span>
                                <input type="file" name="file_data" id="file_input" class="form-control shadow-none"/>
                            </div>
                            <div id="file_error" class="text-danger mt-1 fw-bold" style="display:none;" />
                            <small class="text-muted">Maximum file size: 10MB</small>
                        </div>

                        <button type="submit" class="btn btn-primary mb-3" id="btn">Save Changes</button>
                    </div>
                </div>
            </div>
        </t>
    </template>

</odoo>