<odoo>
    <data>

        <record id="doctor_list" model="ir.ui.view">
            <field name="name">Doctor</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
            <list>
                <field name="doctor_code" width="25px"/>
                <field name="name" width="25px"/>
                <field name="phone" width="30px"/>
                <field name="email" width="30px"/>
                <field name="department_id" width="25px"/>
                <field name="experience" width="25px"/>
                <field name="description" width="25px"/>
                <field name="hospital_id" width="25px"/>
            </list>
            </field>
        </record>


        <record id="doctor_form" model="ir.ui.view">
            <field name="name">Doctor</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <form string="Form View">
                    <sheet>

                        <div name="button_box" position="inside">
                            <button name="appointment_d_btn" type="object" class="oe_stat_button" icon="fa-id-card-o">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">Appointments</span>
                                    <span class="o_stat_value"><field name="appointment_count"/></span>
                                </div>
                            </button>
                        </div>

                        <group>
                            <group>
                                <field name="doctor_code"/>
                                <field name="name"/>
                                <field name="phone" widget="phone"/>
                                <field name="email" widget="email"/>
                                <field name="department_id"/>
                            </group>
                            <group>
                                <field name="experience"/>
                                <field name="description"/>
                                <span>Address</span>
                                <div class="o_address_format">
                                    <field name="street" placeholder="Street..." class="o_address_street"/>
                                    <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                    <field name="city" placeholder="City" class="o_address_city"/>
                                    <field name="state_id" placeholder="State" class="o_address_state" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                                    <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                    <div name="partner_address_country" class="d-flex justify-content-between">
                                        <field name="country_id" placeholder="Country" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                                    </div>
                                </div>
                            </group>
                        </group>

                        <notebook>
                            <page name="hospital" string="Hospital">
                                <field name="hospital_id" options="{'no_create_edit': True}">
                                    <list>
                                        <field name="name"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>

                    </sheet>
                </form>
            </field>
        </record>

        <record id="doctor_action" model="ir.actions.act_window">
            <field name="name">Doctor</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">list,form</field>
            <field name="context">{'is_doctor':1}</field>
            <field name="domain">[('doctor_code','ilike','d%')]</field>
        </record>

        <record model="ir.actions.act_window.view" id="list_doctor_action">
            <field name="view_mode">list</field>
            <field name="view_id" ref="doctor_list"/>
            <field name="act_window_id" ref="doctor_action"/>
        </record>

       <record model="ir.actions.act_window.view" id="form_doctor_action">
            <field name="view_mode">form</field>
            <field name="view_id" ref="doctor_form"/>
            <field name="act_window_id" ref="doctor_action"/>
        </record>

        <menuitem name="Doctor" id="doctor_id" parent="hospital_management.hospital_management_id" action="doctor_action" groups="hospital_management.hospital_group_doctor"/>
    </data>
</odoo>