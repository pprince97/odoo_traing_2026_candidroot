<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="product_taxonomy_template" name="Product taxonomy">
        <div class="accordion-item">
            <div class="accordion-header border-0 border-top border-200 h6 mb-3 pt-3 border">
                <button class="accordion-button px-0 bg-transparent shadow-none"
                        type="button"
                        data-bs-toggle="collapse"
                        aria-expanded="true"
                        t-attf-data-bs-target="#o_wsale_taxonomy_option_inner"
                        t-attf-aria-controls="o_wsale_taxonomy_option_inner">
                    <b>Product Taxonomy</b>
                </button>
            </div>
            <div id="o_wsale_taxonomy_option_inner" class="accordion-collapse collapse show">
                <div class="flex-column mb-3">
                    <t t-call="product_website_jui.filter_product_taxonomy_list">
                        <t t-set="all_tags" t-value="all_tags"/>
                    </t>
                </div>
            </div>
        </div>
    </template>

    <template id="filter_product_taxonomy_list">
        <t t-foreach="all_tags" t-as="tag" class="list-group-item border-0 ps-0 pb-0">
            <div class="form-check mb-1">
                <input type="checkbox"
                       name="tags"
                       class="form-check-input"
                       t-attf-id="tag_#{tag.id}"
                       t-att-value="tag.id"
                       t-att-checked="'checked' if tag.id in tags else None"
                />
                <label class="form-check-label fw-normal" t-attf-for="tag_#{tag.id}" t-field="tag.name"/>
            </div>
        </t>
    </template>

    <template id="product_website_jui.inherited_sidebar" name="Sidebar" inherit_id="website_sale.products">
        <xpath expr="//t[@t-if='show_price_filter']" position="before">
            <t t-call="product_website_jui.product_taxonomy_template"/>
        </xpath>
    </template>
</odoo>